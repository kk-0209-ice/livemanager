# v4.2 更新手順

今回の `signal is aborted without reason` は、
Live Manager側が8秒でWorker通信を中断していたことが原因です。

## 必ず両方更新してください

### 1. Cloudflare Worker
`ticketdive-api-worker-v4.2.txt` の内容を
Cloudflareの `live-manager-ticketdive` → Edit Code へ全部貼り直し、Deploy。

### 2. GitHub Pages
`live-manager-v4.2-worker-timeout-fixed.zip` の中身で
GitHubリポジトリのファイルを上書き。

特に:
- app.js
- sw.js
- index.html

を更新してください。

## 修正内容
- Live Manager側のWorker待機: 8秒 → 12秒
- WorkerからTicketDiveへの各取得: 最大4.5秒
- 2つのUAのうち片方が失敗しても、もう片方が成功すれば結果を返す
- 古いService Workerキャッシュをv4.2へ更新

## 確認
Worker:
`https://あなたのWorker.workers.dev/health`

Live Manager:
その他 → 外部連携設定 → Worker接続テスト
